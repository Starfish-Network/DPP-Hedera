{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#FSMA204PackingEvent&1.0.0",
  "title": "FSMA 204 Packing Event",
  "description": "Source Pydantic: api/app/models/starfish_events.py::PackingEvent. Rules: FSMA_COMMON_001, FSMA_PACKING_001-003.",
  "type": "object",
  "required": ["eventType", "event_time", "facility", "container_id", "input_items"],
  "properties": {
    "eventType": { "const": "packing" },
    "event_time": { "type": "string", "format": "date-time" },
    "event_timezone_offset": { "type": "string", "pattern": "^[+-]\\d{2}:\\d{2}$" },
    "facility": { "type": "string", "minLength": 1 },
    "container_id": { "type": "string", "minLength": 1 },
    "input_items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["epc", "quantity", "unit_of_measurement"],
        "properties": {
          "epc": { "type": "string", "minLength": 1 },
          "quantity": { "type": "number" },
          "unit_of_measurement": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
